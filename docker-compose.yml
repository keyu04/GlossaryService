version: '3.9'

services:

  # ── SQL Server ─────────────────────────────────────────────────
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: glossary_sqlserver
    environment:
      SA_PASSWORD: "Ke@1234!"
      ACCEPT_EULA: "Y"
    ports:
      - "1434:1433"       # host:container — use 1434 to avoid conflict with local SQL
    volumes:
      - sqlserver_data:/var/opt/mssql
    healthcheck:
      test: ["CMD", "/opt/mssql-tools18/bin/sqlcmd", "-S", "localhost",
             "-U", "sa", "-P", "Ke@1234!",
             "-Q", "SELECT 1", "-No"]
      interval: 10s
      retries: 5
      timeout: 5s

  # ── GlossaryService ────────────────────────────────────────────
  glossary-api:
    build:
      context: ./GlossaryService    # ← where your Dockerfile is
      dockerfile: Dockerfile
    container_name: glossary_api
    ports:
      - "5050:8080"       # access API at localhost:5050
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=GlossaryDb;User Id=sa;Password=Ke@1234!;TrustServerCertificate=True;
    depends_on:
      sqlserver:
        condition: service_healthy   # ← wait for SQL Server to be ready
    restart: on-failure

# ── Volumes ────────────────────────────────────────────────────
volumes:
  sqlserver_data: